<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.mapper.FilterKeywordMapper">

    <select id="selectPage" resultType="com.simple.common.entity.filterkeyword.FilterKeywordVO">
        SELECT * FROM filter_keyword
        <where>
            <if test="vo.keyword != null and vo.keyword != ''">
                AND keyword LIKE CONCAT('%',#{vo.keyword},'%')
            </if>
            <if test="vo.type != null and vo.type != ''">
                AND type LIKE CONCAT('%',#{vo.type},'%')
            </if>
            AND deleted = 0
        </where>
    </select>
</mapper>
